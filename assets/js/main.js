const config=window.SITE_CONFIG||{},zh=document.documentElement.lang.toLowerCase().startsWith('zh'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(config.colours){const root=document.documentElement.style;root.setProperty('--accent',config.colours.accent);root.setProperty('--red',config.colours.red);root.setProperty('--bg',config.colours.background);root.setProperty('--dark',config.colours.dark)}
document.querySelectorAll('.mark').forEach(e=>e.textContent=config.initials||'BC');document.querySelectorAll('.wordmark b,.footer b').forEach(e=>e.textContent=config.name||'Brandon Chen');document.querySelectorAll('.wordmark small').forEach(e=>e.textContent=zh?config.tagline?.zh:config.tagline?.en);document.querySelectorAll('a[href^="mailto:"]').forEach(e=>e.href=`mailto:${config.email}`);document.querySelectorAll('a').forEach(e=>{if(e.textContent.trim()==='LinkedIn')e.href=config.linkedin;if(e.textContent.trim()==='ORCID')e.href=config.orcid});
const menu=document.querySelector('.menu'),nav=document.querySelector('.nav');if(menu)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());requestAnimationFrame(()=>document.body.classList.add('is-ready'));
const progress=document.createElement('div');progress.className='scroll-progress';document.body.append(progress);const header=document.querySelector('.site-header');addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%';header?.classList.toggle('is-scrolled',scrollY>12)},{passive:true});
const targets=document.querySelectorAll('.card,.feature>*,.section-heading,.list-item,.contact-panel,.timeline-item,.skill-row,.portfolio-card,.prose>h2,.prose>p');targets.forEach(e=>e.classList.add('reveal-target'));if('IntersectionObserver'in window&&!reduced){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const el=e.target,delay=el.parentElement?[...el.parentElement.children].indexOf(el)*70:0;el.style.transitionDelay=Math.min(delay,420)+'ms';el.classList.add('is-revealed');io.unobserve(el)}}),{threshold:.12,rootMargin:'0px 0px -35px'});targets.forEach(e=>io.observe(e))}else targets.forEach(e=>e.classList.add('is-revealed'));
const parseStat=t=>{const m=t.trim().match(/^([\d.]+)([KMB]?)(\+|×|x)?$/);return m?{n:+m[1],mag:m[2]||'',tail:m[3]||'',dec:m[1].includes('.')?1:0}:null},runCounter=(el,s)=>{const start=performance.now(),tick=now=>{const p=Math.min(1,(now-start)/1100),v=s.n*(1-Math.pow(1-p,3));el.textContent=(s.dec?v.toFixed(1):Math.round(v))+s.mag+s.tail;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)};document.querySelectorAll('.stat-num,.fact b').forEach(el=>{const s=parseStat(el.textContent);if(!s||reduced)return;const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){runCounter(el,s);io.disconnect()}}),{threshold:.5});io.observe(el)});
if(!reduced&&matchMedia('(hover:hover) and (pointer:fine)').matches){document.querySelectorAll('.card,.portfolio-card,.contact-panel').forEach(card=>{let r;card.addEventListener('mouseenter',()=>r=card.getBoundingClientRect());card.addEventListener('mousemove',e=>{const x=(e.clientX-r.left-r.width/2)/(r.width/2),y=(e.clientY-r.top-r.height/2)/(r.height/2);card.style.transform=`translateY(-5px) perspective(1100px) rotateX(${-y*5.5}deg) rotateY(${x*5.5}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')});const glow=document.createElement('div');glow.className='cursor-glow';document.body.append(glow);addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'})}
const hero=document.querySelector('.hero-inner');if(hero){const ticker=document.createElement('div');ticker.className='gnss-ticker';ticker.innerHTML=`<span>GNSS <b data-sat>-- / 24</b></span><span>HDOP <b data-hdop>--</b></span><span>MODE <b>${zh?'城市':'URBAN'}</b></span>`;hero.append(ticker);const type=(el,text)=>{clearInterval(el._t);el.textContent='';el.classList.add('is-typing');let i=0;el._t=setInterval(()=>{if(i>=text.length){clearInterval(el._t);el.classList.remove('is-typing')}else el.textContent+=text[i++]},38)},tick=()=>{type(ticker.querySelector('[data-sat]'),(11+Math.floor(Math.random()*6))+' / 24');setTimeout(()=>type(ticker.querySelector('[data-hdop]'),(0.7+Math.random()*.5).toFixed(1)),280)};tick();if(!reduced)setInterval(tick,2800)}
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:reduced?'auto':'smooth'})}}));document.querySelectorAll('a[href]').forEach(a=>a.addEventListener('click',e=>{if(e.defaultPrevented||a.target==='_blank'||a.hasAttribute('download')||a.href.startsWith('mailto:')||a.origin!==location.origin||a.hash)return;e.preventDefault();document.body.classList.add('is-leaving');setTimeout(()=>location.href=a.href,220)}));
